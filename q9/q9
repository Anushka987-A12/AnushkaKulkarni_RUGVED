def encrypt(message,shift):
    a=message.lower()
    l=""
    for i in a:
        if i.islower():
            l+=chr((ord(i)+shift-97)%26 + 97)
        else:
            l+=i
    print(l)
m=input("Enter text to encrypt: ")
s=int(input("Enter no to shift by: "))
encrypt(m,s)
