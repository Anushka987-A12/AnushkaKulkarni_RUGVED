x=input("Enter a string :")
y=list(x)
z=len(y)
pos=0
for i in range(z):
    s=y[i]
    for j in range(i+1,z):
        if y[j] < s:
            s=y[j]
            pos=j
    y[pos]=y[i]
    y[i]=s

print(''.join(y))


    
